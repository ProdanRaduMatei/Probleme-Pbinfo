#include <iostream>

using namespace std;

int mod(int x) {
    if (x >= 0)
        return x;
    return -x;
}

int main()
{
    int n, a, b, d;
    cin >> n;
    cin >> a;
    for (int i = 1; i <)
    return 0;
}
