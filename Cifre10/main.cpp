#include <iostream>
#include <fstream>

using namespace std;

ifstream fin("cifre10.in");
ofstream fout("cifre.out");
int n, a, b, C[10], c[10], cnt, p, m, u;
int main()
{
    cin >> p >> n;
    for (int i = 1; i <= n; ++i) {
        cin >> a >> b;
        while ()
    }
    return 0;
}
