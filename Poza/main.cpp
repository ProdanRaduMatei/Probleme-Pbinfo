#include <iostream>

using namespace std;

int main()
{
    int n, m, v[101][101] = {0};
    cin >> n >> m;
    for (int i = 1; i <= n; ++i)
        for (int j = 1; j <= m; ++j) {
            fin >> v[i][j];
            if (v[i][j] == 1)

        }

    return 0;
}
