#include <iostream>

using namespace std;

int a[1005][1005], n, m, k, a1, b, c, d, s, smax;
int af, bf, cf, df;

int main()
{
    cin >> n >> m >> k;
    for (int i = 1; i <= n; ++i)
        for (int j = 1; j <= m; ++j)
            cin >> a[i][j];
    for (int i = 1; i <= k; ++i) {
        cin >> a1 >> b >> c >> d;
        if (!a1 >= af) || !(b >= bf) || !(c <= cf) || !(d <= df) {
            s = 0;
            for (int )
        }
    }
    return 0;
}
