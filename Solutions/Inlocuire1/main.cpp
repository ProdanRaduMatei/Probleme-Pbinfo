#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    char s[256];
    int c = 0;
    cin.getline(s, 256);
    for (int i = 0; i < strlen(s); ++i) {
        while ()
    }
    return 0;
}
