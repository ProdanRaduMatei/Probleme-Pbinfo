#include <bits/stdc++.h>

using namespace std;

ifstream fin("bisectoare.in");
ofstream fout("bisectoare.out");

int main()
{
    int s = 0, a;
    while (fin >> a) {
        s = s + a;
    }

    return 0;
}
